# 全部取最新版。注意：需要 Python >= 3.10。
# Python 3.9 上 torch 最高只到 2.2.2（官方未再为 3.9 发布 macOS arm64 wheel），
# 而 torch 2.2.2 不兼容 numpy 2.x —— 在 3.9 上无法做到全部最新。
numpy
opencv-python-headless
matplotlib
PyYAML
torch
torchaudio
scikit-learn
panns_inference
